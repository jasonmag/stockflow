<div class="mb-6 flex flex-wrap items-end justify-between gap-3">
  <div>
    <h1 class="page-title"><%= @customer.name %></h1>
    <p class="mt-1 text-sm text-slate-600">Customer profile and contact details.</p>
  </div>
  <div class="flex flex-wrap gap-2">
    <%= link_to "Edit", edit_customer_path(@customer), class: "btn btn-primary" %>
    <%= link_to "Back", customers_path, class: "btn btn-secondary" %>
  </div>
</div>

<section class="card">
  <dl class="grid gap-4 sm:grid-cols-2">
    <div>
      <dt class="kpi-label">Contact Person</dt>
      <dd class="mt-1 text-sm text-slate-900"><%= @customer.contact_name.presence || "-" %></dd>
    </div>
    <div>
      <dt class="kpi-label">Email</dt>
      <dd class="mt-1 text-sm text-slate-900"><%= @customer.contact_email.presence || "-" %></dd>
    </div>
    <div>
      <dt class="kpi-label">Phone</dt>
      <dd class="mt-1 text-sm text-slate-900"><%= @customer.contact_phone.presence || "-" %></dd>
    </div>
    <div class="sm:col-span-2">
      <dt class="kpi-label">Address</dt>
      <dd class="mt-1 text-sm text-slate-900"><%= @customer.address.presence || "-" %></dd>
    </div>
  </dl>
</section>

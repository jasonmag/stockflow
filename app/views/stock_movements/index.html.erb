<div class="mb-6 flex flex-wrap items-end justify-between gap-3">
  <div>
    <h1 class="page-title">Stock Movements</h1>
    <p class="mt-1 text-sm text-slate-600">Inventory ledger of stock in, out, transfers, and adjustments.</p>
  </div>
  <div class="flex flex-wrap gap-2">
    <%= link_to "Manual Counts", stock_count_sessions_path, class: "btn btn-secondary" %>
    <%= link_to "Self Consumption", new_self_consumption_stock_movements_path(reason_code: :self_consumption), class: "btn btn-secondary" %>
    <%= link_to "Spoilage", new_spoilage_stock_movements_path(reason_code: :spoilage), class: "btn btn-secondary" %>
    <%= link_to "New Stock Movement", new_stock_movement_path, class: "btn btn-primary" %>
  </div>
</div>

<% if @current_inventory_rows.any? %>
  <section class="card mb-4">
    <h2 class="mb-3 text-base font-semibold text-slate-900">Current Counts By Location</h2>
    <div class="space-y-4">
      <% @current_inventory_rows.each do |location_name, rows| %>
        <section class="rounded-lg border border-slate-200">
          <div class="border-b border-slate-200 bg-slate-50 px-4 py-3">
            <h3 class="text-sm font-semibold text-slate-900"><%= location_name %></h3>
          </div>
          <div class="table-wrap">
            <table class="table-base">
              <thead>
                <tr>
                  <th>Product</th>
                  <th>Current Count</th>
                </tr>
              </thead>
              <tbody>
                <% rows.each do |row| %>
                  <tr>
                    <td data-label="Product" class="font-medium text-slate-900"><%= row[:product_name] %></td>
                    <td data-label="Current Count"><%= number_with_precision(row[:quantity], precision: 2, strip_insignificant_zeros: true) %></td>
                  </tr>
                <% end %>
              </tbody>
            </table>
          </div>
        </section>
      <% end %>
    </div>
  </section>
<% end %>

<% if @stock_movements.any? %>
  <div class="table-wrap">
    <table class="table-base">
      <thead>
        <tr>
          <th>Date</th>
          <th>Type</th>
          <th>Product</th>
          <th>Reason</th>
          <th>Quantity</th>
          <th>From</th>
          <th>To</th>
        </tr>
      </thead>
      <tbody>
        <% @stock_movements.each do |row| %>
          <tr>
            <td data-label="Date"><%= row.occurred_on.to_fs(:long) %></td>
            <td data-label="Type"><%= status_badge(row.movement_type) %></td>
            <td data-label="Product" class="font-medium text-slate-900"><%= row.product.name %></td>
            <td data-label="Reason"><%= row.reason_code.present? ? status_badge(row.reason_code) : "-" %></td>
            <td data-label="Quantity"><%= row.quantity %></td>
            <td data-label="From"><%= row.from_label %></td>
            <td data-label="To"><%= row.to_label %></td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>
<% else %>
  <p class="empty-state">No stock movements yet. Add one to start your inventory ledger.</p>
<% end %>
